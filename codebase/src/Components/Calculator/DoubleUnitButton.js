import React from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

const DoubleUnitButton = ({ props }) => {};

DoubleUnitButton.propTypes = {
  props: PropTypes.object,
};

export default DoubleUnitButton;
