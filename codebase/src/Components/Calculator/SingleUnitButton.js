import React from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

const SingleUnitButton = ({ props }) => {};

SingleUnitButton.propTypes = {
  props: PropTypes.object,
};

export default SingleUnitButton;
